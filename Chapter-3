**3.1 Aşağıdaki cümlelerdeki boşlukları doldurun:**

a) Bir **sol küme parantezi ({)** her metodun gövdesini başlatır ve bir **sağ küme parantezi (})** her metodun gövdesini sonlandırır.
b) Çoğu ifade bir **noktalı virgül (;)** ile biter.
c) **if** ifadesi karar vermek için kullanılır.
d) **//** tek satırlık bir yorum başlatır.
e) **Boş satırlar**, **boşluk karakterleri** ve **sekme karakterleri (tabs)** boşluk (whitespace) olarak adlandırılır. Yeni satır karakterleri de boşluk olarak kabul edilir.
f) **Anahtar kelimeler (Keywords)** C# tarafından kullanılmak üzere ayrılmıştır.
g) C# uygulamaları **Main** metodunda yürütülmeye başlar.
h) **Console.WriteLine** ve **Console.Write** metodları konsol penceresinde bilgi görüntüler.
i) **Dize enterpolasyonu (String interpolation)** değerleri doğrudan bir dize değişmezi içine eklemenizi sağlar.

**3.2 Aşağıdaki ifadelerin her birinin doğru mu yanlış mı olduğunu belirtin. Yanlışsa, nedenini açıklayın:**

a) Yorumlar, uygulama yürütüldüğünde bilgisayarın `//` sonrasındaki metni ekranda görüntülemesine neden olur.
    * **Yanlış.** Yorumlar, uygulama yürütüldüğünde herhangi bir işlemin yapılmasına neden olmaz. Uygulamaları belgelemek ve okunabilirliğini artırmak için kullanılırlar.

b) C#, `number` ve `NuMbEr` değişkenlerini aynı kabul eder.
    * **Yanlış.** C# büyük/küçük harfe duyarlıdır, bu nedenle bu değişkenler farklıdır.

c) Kalan (modulus) operatörü (%) yalnızca tamsayı işlenenlerle kullanılabilir.
    * **Yanlış.** Kalan operatörü C#'ta tam sayı olmayan işlenenlerle de kullanılabilir.

d) Aritmetik operatörler `*`, `/`, `%`, `+` ve `-` hepsi aynı öncelik seviyesine sahiptir.
    * **Yanlış.** `*`, `/` ve `%` operatörleri aynı öncelik seviyesine sahiptir ve `+` ve `-` operatörleri daha düşük bir öncelik seviyesine sahiptir.

e) Enterpolasyonlu bir dize, dize değişmezinden önce bir `$` ile başlamalıdır.
    * **Doğru.**

**3.3 Aşağıdaki görevlerin her birini gerçekleştirmek için ifadeler yazın:**

a) `c`, `thisIsAVariable`, `q76354` ve `number` değişkenlerini `int` türünde bildirin.
```csharp
int c, thisIsAVariable, q76354, number;

b) Kullanıcıdan bir tamsayı girmesini isteyin.
Console.Write("Bir tamsayı girin: ");

c) Bir tamsayı girin ve sonucu int değişkeni value'ya atayın.
value = int.Parse(Console.ReadLine());

d) Eğer number değişkeni 7'ye eşit değilse, "The variable number is not equal to 7" yazdırın.
if (number != 7)
{
    Console.WriteLine("The variable number is not equal to 7");
}

e) Konsol penceresinde tek bir satırda "This is a C# app" yazdırın.
Console.WriteLine("This is a C# app");

f) Konsol penceresinde iki satırda "This is a C# app" yazdırın. İlk satır C# ile bitmelidir. Console.WriteLine metodunu kullanın.
Console.WriteLine("This is a C#\napp");

g) x ve y değişkenlerinin toplamını görüntülemek için dize enterpolasyonu kullanan bir ifade yazın. x ve y değişkenlerinin mevcut olduğunu ve değerlere sahip olduğunu varsayın.
Console.WriteLine($"The sum of {x} and {y} is {x + y}");

3.4 Aşağıdaki kod parçalarındaki hataları bulun ve düzeltin:

a)


if (c < 7);
{
    Console.WriteLine("c is less than 7");
}
Hata: if ifadesindeki koşul (c < 7)'dan sonraki noktalı virgül.

Düzeltme: Sağ parantezden sonraki noktalı virgülü kaldırın.

b)


if (c => 7)
{
    Console.WriteLine("c is equal to or greater than 7");
}
Hata: İlişkisel operatör => yanlıştır.

Düzeltme: => yerine >= kullanın.

3.5 Aşağıdaki görevlerin her birini gerçekleştiren bildirimler, ifadeler veya yorumlar yazın:

a) Bir uygulamanın üç tamsayının çarpımını hesaplayacağını belirtin.
// Üç tamsayının çarpımını hesaplama
b) x, y, z ve result değişkenlerini int türünde bildirin.
int x, y, z, result;

c) Kullanıcıdan ilk tamsayıyı girmesini isteyin.
Console.Write("Enter first integer: ");

d) Kullanıcıdan ilk tamsayıyı okuyun ve x değişkeninde saklayın.
x = int.Parse(Console.ReadLine());

e) Kullanıcıdan ikinci tamsayıyı girmesini isteyin.
Console.Write("Enter second integer: ");

f) Kullanıcıdan ikinci tamsayıyı okuyun ve y değişkeninde saklayın.
y = int.Parse(Console.ReadLine())
g) Kullanıcıdan üçüncü tamsayıyı girmesini isteyin.
Console.Write("Enter third integer: ");

h) Kullanıcıdan üçüncü tamsayıyı okuyun ve z değişkeninde saklayın.
z = int.Parse(Console.ReadLine());

i) x, y ve z değişkenlerindeki üç tamsayının çarpımını hesaplayın ve sonucu result değişkenine atayın.
result = x * y * z;

j) "Product is" mesajını ve ardından result değişkeninin değerini görüntüleyin—dize enterpolasyonu kullanın.
Console.WriteLine($"Product is {result}");

3.6 Alıştırma 3.5'te yazdığınız ifadeleri kullanarak, üç tamsayının çarpımını hesaplayan ve görüntüleyen tam bir uygulama yazın.

C#

using System;

class Product
{
   static void Main()
   {
      int x, y, z, result;

      Console.Write("Enter first integer: ");
      x = int.Parse(Console.ReadLine());

      Console.Write("Enter second integer: ");
      y = int.Parse(Console.ReadLine());

      Console.Write("Enter third integer: ");
      z = int.Parse(Console.ReadLine());

      result = x * y * z;

      Console.WriteLine($"Product is {result}");
   }
}

Bölüm 3: Alıştırmalar (Exercises 3.7 - 3.32)
3.7 Boşlukları doldurun:

a) Her using direktifi, bir C# uygulamasının kullanabileceği sınıfları içeren bir ad alanını (namespace) tanımlar. b) / ve ***** sembolleri C#'ta sınırlandırılmış yorumlar (delimited comments) içindir. c) Hesaplamalar normalde atama (assignment) ifadeleriyle gerçekleştirilir. d) Çarpma ile aynı önceliğe sahip aritmetik operatörler bölme (/) ve kalan (%)'dır. e) Bir aritmetik ifadede parantezler iç içe geçtiğinde, en içteki (innermost) parantez kümesi önce değerlendirilir. f) Bilgisayarın belleğinde, bir uygulamanın yürütülmesi boyunca çeşitli zamanlarda farklı değerler içerebilen bir konuma değişken (variable) denir.

3.8 Aşağıdaki görevlerin her birini gerçekleştiren C# ifadeleri yazın:

a) "Roll Number Student Name Grade" ifadesini tek bir satırda sekme duraklarıyla (tabs) ayrılmış olarak görüntüleyin.

C#

Console.WriteLine("Roll Number\tStudent Name\tGrade");
b) Bir dizede çift tırnak karakteri (") görüntüleyin.

C#

Console.WriteLine("\"");
c) Bir dize değerini başlangıçta saklamak için bir değişken bildirin.

C#

string myString = "initial value";
3.9 Aşağıdaki ifadelerin her birinin doğru mu yanlış mı olduğunu belirtin. Yanlışsa, nedenini açıklayın:

a) IntelliSense, o ana kadar yazılan harflerle başlayan veya bunları içeren çeşitli öğeleri listeler. * Doğru.

b) Aşağıdakilerin hepsi geçerli değişken adlarıdır: _under_bar_, m928134, t5, j7, her_sales, his_account_total, a, b, c, z ve z2. * Doğru. (Hepsi geçerli tanımlayıcılardır).

c) Parantez içermeyen geçerli bir C# aritmetik ifadesi soldan sağa doğru değerlendirilir. * Yanlış. Operatör önceliği kurallarına göre değerlendirilir. Aynı önceliğe sahip operatörler soldan sağa değerlendirilir (atama hariç).

d) void anahtar kelimesi, Main metodunun görevini tamamladıktan sonra bir değer döndürmeyeceğini belirtir. * Doğru.

3.10 x = 2 ve y = 3 olduğunu varsayarsak, aşağıdaki ifadelerin her biri ne görüntüler?

a) Console.WriteLine($"x = {x}"); * Çıktı: x = 2

b) Console.WriteLine($"Value of {x} + {x} is {x + x}"); * Çıktı: Value of 2 + 2 is 4

c) Console.Write("x ="); * Çıktı: x =

d) Console.WriteLine($"{x + y} = {y + x}"); * Çıktı: 5 = 5

3.11 Aşağıdaki C# ifadelerinden hangileri değerleri değiştirilen değişkenler içerir?

a) p = i + j + k + 7; * Değiştirilen: p

b) Console.WriteLine("variables whose values are modified"); * Hiçbiri.

c) Console.WriteLine("a = 5"); * Hiçbiri. (Bu sadece bir metin çıktısıdır).

d) value = int.Parse(Console.ReadLine()); * Değiştirilen: value

3.12 Aşağıdaki matematiksel ifadeler için eşdeğer C# kodunu yazın:

a) y = 2ab + 2ac
y = 2 * a * b + 2 * a * c;

b) z = x(1 + x) - y(1 + y)
z = x * (1 + x) - y * (1 + y);

c) x = xy / 3
x = x * y / 3;

d) y = 4ab / (4a + 4b)
y = (4 * a * b) / (4 * a + 4 * b);

e) z = x * a^3 + y * b^3 (Not: C#'ta üs alma operatörü yoktur, Math.Pow veya çarpma kullanılır)
z = x * a * a * a + y * b * b * b; 
// Veya: z = x * Math.Pow(a, 3) + y * Math.Pow(b, 3);

f) x = ac * (bd)^4

x = a * c * (b * d) * (b * d) * (b * d) * (b * d);
// Veya: x = a * c * Math.Pow(b * d, 4);
3.13 (Değerlendirme Sırası) Aşağıdaki C# ifadelerindeki operatörlerin değerlendirilme sırasını belirtin ve her ifade gerçekleştirildikten sonra x'in değerini gösterin:

a) x = 7 + 3 * 6 / 2 - 1; * Sıra: *, /, +, -, = * Hesaplama: 7 + 18 / 2 - 1 -> 7 + 9 - 1 -> 16 - 1 -> 15 * x'in değeri: 15

b) x = 2 % 2 + 2 * 2 - 2 / 2; * Sıra: %, *, /, +, -, = * Hesaplama: 0 + 4 - 1 -> 3 * x'in değeri: 3

c) x = (3 * 9 * (3 + (9 * 3 / (3)))); * Sıra: Parantez içleri öncelikli (en içten dışa), sonra çarpma/bölme. * (3) -> 3 * 9 * 3 / 3 -> 9 * 3 + 9 -> 12 * 3 * 9 * 12 -> 324 * x'in değeri: 324

3.14 (Sayıları Görüntüleme) 1'den 4'e kadar olan sayıları aynı satırda, her bir çift bitişik sayı bir boşlukla ayrılmış olarak görüntüleyen bir uygulama yazın. Uygulamayı aşağıdaki teknikleri kullanarak yazın:

a) Bir Console.WriteLine ifadesi kullanın.
Console.WriteLine("1 2 3 4");

b) Dört Console.Write ifadesi kullanın.
Console.Write("1 ");
Console.Write("2 ");
Console.Write("3 ");
Console.Write("4");

c) Dört int değişkeni ve dize enterpolasyonu ile bir Console.WriteLine ifadesi kullanın.
int a=1, b=2, c=3, d=4;
Console.WriteLine($"{a} {b} {c} {d}");


3.15 Alışveriş (Shopping)
Kullanıcıdan üç ürünün fiyatını girmesini isteyen, toplamı hesaplayan, %10 indirim uygulayan ve net ödenecek tutarı gösteren bir uygulama yazın.

using System;

class Shopping
{
    static void Main()
    {
        Console.Write("Enter price of item 1: ");
        // Para birimi için decimal kullanmak daha doğrudur.
        decimal price1 = decimal.Parse(Console.ReadLine()); 

        Console.Write("Enter price of item 2: ");
        decimal price2 = decimal.Parse(Console.ReadLine());

        Console.Write("Enter price of item 3: ");
        decimal price3 = decimal.Parse(Console.ReadLine());

        decimal total = price1 + price2 + price3;
        decimal discount = total * 0.10m; // 0.10m, decimal literal'ı belirtir
        decimal netPayable = total - discount;

        Console.WriteLine($"\n--- Invoice Summary ---");
        // :C formatı para birimini gösterir (Türkiye'de ₺ olarak görünebilir)
        Console.WriteLine($"Total: {total:C}"); 
        Console.WriteLine($"Discount (10%): {discount:C}"); 
        Console.WriteLine($"Net Payable: {netPayable:C}");
    }
}

3.16 Hafta İçi/Hafta Sonu (Weekday/Weekend)
Kullanıcıdan 1'den 7'ye kadar bir tamsayı (gün numarası) girmesini isteyen ve günün hafta içi, hafta sonu veya geçersiz olup olmadığını kontrol eden bir uygulama yazın.

using System;

class WeekdayWeekend
{
    static void Main()
    {
        Console.Write("Enter an integer for the day (1-7): ");
        int day = int.Parse(Console.ReadLine());

        if (day >= 1 && day <= 5)
        {
            Console.WriteLine("working day");
        }
        
        // Mantıksal OR operatörü (||) ile 6 veya 7 olma durumu kontrol edilir.
        if (day == 6 || day == 7)
        {
            Console.WriteLine("weekend");
        }

        // Geçersiz giriş
        if (day < 1 || day > 7)
        {
            Console.WriteLine("invalid");
        }
    }
}

3.17 Aritmetik, En Küçük ve En Büyük (Arithmetic, Smallest and Largest)
Kullanıcıdan üç farklı tamsayı girmesini isteyen, bu sayıların toplamını, ortalamasını, çarpımını, en küçüğünü ve en büyüğünü hesaplayıp görüntüleyen bir uygulama yazın.

using System;

class Arithmetic
{
    static void Main()
    {
        Console.Write("Enter first integer: ");
        int num1 = int.Parse(Console.ReadLine());

        Console.Write("Enter second integer: ");
        int num2 = int.Parse(Console.ReadLine());

        Console.Write("Enter third integer: ");
        int num3 = int.Parse(Console.ReadLine());

        // Aritmetik Hesaplamalar
        int sum = num1 + num2 + num3;
        int average = sum / 3; 
        int product = num1 * num2 * num3;

        // En Küçüğü Bulma
        int smallest = num1; // Başlangıçta ilk sayı en küçük kabul edilir
        if (num2 < smallest) smallest = num2;
        if (num3 < smallest) smallest = num3;

        // En Büyüğü Bulma
        int largest = num1; // Başlangıçta ilk sayı en büyük kabul edilir
        if (num2 > largest) largest = num2;
        if (num3 > largest) largest = num3;

        // Sonuçları Görüntüleme
        Console.WriteLine($"\nSum: {sum}");
        Console.WriteLine($"Average: {average}");
        Console.WriteLine($"Product: {product}");
        Console.WriteLine($"Smallest: {smallest}");
        Console.WriteLine($"Largest: {largest}");
    }
}

3.18 Şekilleri Yıldızlarla Görüntüleme (Displaying Shapes with Asterisks)
Aşağıdaki dört şekli, tek bir Console.WriteLine ifadesi kullanarak ekranda görüntüleyen bir uygulama yazın.
using System;

class Shapes
{
    static void Main()
    {
        // Şekilleri oluşturmak için \n (yeni satır) ve \t (sekme) karakterleri kullanılır.
        Console.WriteLine("*********\t***\t  *\t    *\n* *\t* *\t * *\t   * *\n* *\t* *\t* *\t  * *\n* *\t* *\t* *\t * *\n* *\t* *\t* *\t* *\n* *\t* *\t* *\t * *\n* *\t* *\t* *\t  * *\n* *\t* *\t * *\t   * *\n*********\t***\t  *\t    *");
    }
}
3.24 Tek mi Çift mi (Odd or Even)
Kullanıcıdan bir tamsayı girmesini isteyen ve sayının tek mi yoksa çift mi olduğunu belirleyen bir uygulama yazın.
using System;

class OddOrEven
{
    static void Main()
    {
        Console.Write("Enter an integer: ");
        int number = int.Parse(Console.ReadLine());

        // Kalan 0 ise çift (Even), değilse tektir (Odd).
        if (number % 2 == 0)
        {
            Console.WriteLine("Even");
        }
        
        if (number % 2 != 0)
        {
            Console.WriteLine("Odd");
        }
    }
}
3.25 Katlar (Multiples)
Kullanıcıdan iki tamsayı girmesini isteyen ve ilk sayının ikincinin bir katı olup olmadığını belirleyen bir uygulama yazın.
using System;

class Multiples
{
    static void Main()
    {
        Console.Write("Enter first integer: ");
        int num1 = int.Parse(Console.ReadLine());

        Console.Write("Enter second integer: ");
        int num2 = int.Parse(Console.ReadLine());

        // num1'in num2'ye bölümünden kalan 0 ise katıdır.
        if (num2 != 0 && num1 % num2 == 0)
        {
            Console.WriteLine($"{num1} is a multiple of {num2}");
        }
        else
        {
            Console.WriteLine($"{num1} is NOT a multiple of {num2}");
        }
    }
}
3.26 Daire Çapı, Çevresi ve Alanı (Circle Diameter, Circumference and Area)
Kullanıcıdan dairenin yarıçapını (tamsayı olarak) girmesini isteyen, ardından çapını, çevresini ve alanını görüntüleyen bir uygulama yazın.

using System;

class Circle
{
    static void Main()
    {
        Console.Write("Enter radius (integer): ");
        int r = int.Parse(Console.ReadLine());

        // Math.PI double tipindedir. Hesaplamalar da double tipinde olmalıdır.
        double diameter = 2 * r;
        double circumference = 2 * Math.PI * r;
        double area = Math.PI * r * r;

        Console.WriteLine($"\nRadius: {r}");
        Console.WriteLine($"Diameter: {diameter}");
        Console.WriteLine($"Circumference: {circumference}");
        Console.WriteLine($"Area: {area}");
    }
}
3.27 Karakterin Tamsayı Karşılığı (Value of a Character)
Aşağıdaki karakterlerin tamsayı karşılıklarını görüntüleyen bir uygulama yazın: 'A', 'B', 'C', 'a', 'b', 'c', '0', '1', '2', '$', '*', '+', '/' ve boşluk karakteri.

using System;

class CharToInt
{
    static void Main()
    {
        // Karakterin tamsayı (ASCII/Unicode) değerini almak için (int) tür dönüştürmesi kullanılır.
        Console.WriteLine($"The integer equivalent of 'A' is {(int)'A'}");
        Console.WriteLine($"The integer equivalent of 'B' is {(int)'B'}");
        Console.WriteLine($"The integer equivalent of 'C' is {(int)'C'}");
        Console.WriteLine($"The integer equivalent of 'a' is {(int)'a'}");
        Console.WriteLine($"The integer equivalent of 'b' is {(int)'b'}");
        Console.WriteLine($"The integer equivalent of 'c' is {(int)'c'}");
        Console.WriteLine($"The integer equivalent of '0' is {(int)'0'}");
        Console.WriteLine($"The integer equivalent of '1' is {(int)'1'}");
        Console.WriteLine($"The integer equivalent of '2' is {(int)'2'}");
        Console.WriteLine($"The integer equivalent of '$' is {(int)'$'}");
        Console.WriteLine($"The integer equivalent of '*' is {(int)'*'}");
        Console.WriteLine($"The integer equivalent of '+' is {(int)'+'}");
        Console.WriteLine($"The integer equivalent of '/' is {(int)'/'}");
        Console.WriteLine($"The integer equivalent of ' ' is {(int)' '}");
    }
}
3.28 Bir Tamsayının Basamakları (Separating Digits in an Integer)
Kullanıcıdan beş basamaklı bir tamsayı girmesini isteyen ve her basamağı üç boşlukla ayırarak görüntüleyen bir uygulama yazın.

using System;

class Digits
{
    static void Main()
    {
        Console.Write("Enter a 5-digit number: ");
        int number = int.Parse(Console.ReadLine());

        // Basamakları matematiksel operatörlerle ayırma
        int digit1 = number / 10000;         
        int digit2 = (number / 1000) % 10;   
        int digit3 = (number / 100) % 10;    
        int digit4 = (number / 10) % 10;     
        int digit5 = number % 10;            

        // Basamakları üç boşlukla ayırarak görüntüleme
        Console.WriteLine($"{digit1}   {digit2}   {digit3}   {digit4}   {digit5}");
    }
}
3.29 Çarpım Tablosu (Multiplication Table)
Kullanıcıdan bir tamsayı girmesini isteyen ve bu sayının 1'den 10'a kadar çarpım tablosunu görüntüleyen bir uygulama yazın.

using System;

class MultiplicationTable
{
    static void Main()
    {
        Console.Write("Input a number: ");
        int n = int.Parse(Console.ReadLine());

        Console.WriteLine($"\nThe Table of {n} is:-");
        Console.WriteLine($"{n} X 1 = {n * 1}");
        Console.WriteLine($"{n} X 2 = {n * 2}");
        Console.WriteLine($"{n} X 3 = {n * 3}");
        Console.WriteLine($"{n} X 4 = {n * 4}");
        Console.WriteLine($"{n} X 5 = {n * 5}");
        Console.WriteLine($"{n} X 6 = {n * 6}");
        Console.WriteLine($"{n} X 7 = {n * 7}");
        Console.WriteLine($"{n} X 8 = {n * 8}");
        Console.WriteLine($"{n} X 9 = {n * 9}");
        Console.WriteLine($"{n} X 10 = {n * 10}");
    }
}
3.30 Negatif, Pozitif ve Sıfır Değerleri Sayma (Counting Negative, Positive and Zero Values)
Kullanıcıdan beş tamsayı girmesini isteyen ve kaç tanesinin negatif, pozitif ve sıfır olduğunu sayıp görüntüleyen bir uygulama yazın.

using System;

class CountValues
{
    static void Main()
    {
        int neg = 0, pos = 0, zero = 0; // Sayaçlar

        for (int i = 1; i <= 5; i++)
        {
            Console.Write($"Enter number {i}: ");
            int n = int.Parse(Console.ReadLine());

            if (n < 0) neg++;
            else if (n > 0) pos++;
            else zero++;
        }
        
        // for döngüsü kullanılmamış versiyonu (if serisi)
        /*
        Console.Write("Enter number 1: "); int n1 = int.Parse(Console.ReadLine());
        if (n1 < 0) neg++; else if (n1 > 0) pos++; else zero++;
        // ... diğer 4 sayı için benzer kod
        */
        
        Console.WriteLine($"\nNegative: {neg}");
        Console.WriteLine($"Positive: {pos}");
        Console.WriteLine($"Zeros: {zero}");
    }
}
3.31 Bütçeleme (Budgeting)
Kullanıcıdan aylık gelir ve harcamaları girmesini isteyen, tasarrufu ve tasarruf oranını hesaplayan, orana göre yorum yapan bir uygulama yazın.

using System;

class Budgeting
{
    static void Main()
    {
        Console.Write("Enter monthly income: ");
        decimal income = decimal.Parse(Console.ReadLine());

        Console.Write("Enter monthly expenses: ");
        decimal expenses = decimal.Parse(Console.ReadLine());

        decimal savings = income - expenses;
        decimal ratio = 0;
        if (income > 0) ratio = savings / income;

        Console.WriteLine($"\nMonthly Savings: {savings:C}");
        Console.WriteLine($"Savings Ratio: {ratio:P}"); // :P ile yüzde formatı

        // Orana göre yorum yapma
        if (ratio < 0.20m) 
            Console.WriteLine("Budget Comment: Extravagant or Low on Savings");
        else if (ratio < 0.40m) 
            Console.WriteLine("Budget Comment: Less Extravagant or Moderate on Savings");
        else if (ratio < 0.75m) 
            Console.WriteLine("Budget Comment: Lively or Strong on Savings");
        else // ratio >= 0.75m
            Console.WriteLine("Budget Comment: Miser or Mad about Savings");
    }
}
3.32 Araba Paylaşımı Tasarruf Hesaplayıcı (Car-Pool Savings Calculator)
Kullanıcıdan bir dizi günlük sürüş maliyeti bilgisini girmesini isteyen ve toplam günlük sürüş maliyetini hesaplayıp görüntüleyen bir uygulama yazın.

using System;

class CarPoolSavings
{
    static void Main()
    {
        Console.Write("Total miles driven per day: ");
        double miles = double.Parse(Console.ReadLine());

        Console.Write("Cost per gallon of gasoline (in cents): ");
        double costPerGallon = double.Parse(Console.ReadLine());

        Console.Write("Average miles per gallon: ");
        double mpg = double.Parse(Console.ReadLine());

        Console.Write("Parking fees per day (in cents): ");
        double parking = double.Parse(Console.ReadLine());

        Console.Write("Tolls per day (in cents): ");
        double tolls = double.Parse(Console.ReadLine());

        // Hesaplama
        // 1. Günlük Yakıt Maliyeti (kuruş cinsinden)
        double gasolineCost = (miles / mpg) * costPerGallon;
        
        // 2. Toplam Günlük Maliyet (kuruş cinsinden)
        double dailyCostInCents = gasolineCost + parking + tolls;
        
        // 3. Sonucu dolar/TL cinsine çevirme (kuruş/cent / 100)
        double dailyCost = dailyCostInCents / 100;

        Console.WriteLine($"\nTotal daily driving cost: {dailyCost:C}");
    }
}
